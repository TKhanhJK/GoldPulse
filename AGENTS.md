# Lưu ý

- Sau mỗi lần hoàn thành thay đổi, bắt buộc phải tạo một Git commit tương ứng để thuận tiện cho việc theo dõi và khôi phục (rollback) sau này.
- Sau mỗi lần thay đổi, bắt buộc phải viết hoặc cập nhật các bài kiểm thử (test) liên quan, đồng thời trước khi bàn giao cho người dùng, phải đảm bảo tất cả các bài kiểm thử và quá trình xác minh đều được thông qua.
